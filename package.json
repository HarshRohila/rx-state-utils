{
  "name": "rx-state-utils",
  "version": "1.0.0",
  "description": "Utilities for State Management using RxJS",
  "files": [
    "dist"
  ],
  "main": "dist/bundle.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/index.ts --bundle --outfile=dist/bundle.js --minify --sourcemap --external:rxjs",
    "build:types": "tsc --emitDeclarationOnly --outDir dist",
    "prepublishOnly": "npm run build"
  },
  "repository": "https://github.com/HarshRohila/rx-state-utils",
  "keywords": [
    "rxjs",
    "state management"
  ],
  "author": "Harsh Rohila",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "0.20.2",
    "rxjs": "7",
    "typescript": "^5.4.3"
  },
  "peerDependencies": {
    "rxjs": "7"
  },
  "volta": {
    "node": "20.12.0"
  }
}
